[features]
seeds = false
skip-lint = false

[programs.devnet]
gig3_escrow = "GIG3EscrowProgramIDChangeBeforeDeployment11111"

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "devnet"
wallet = "~/.config/solana/id.json"

[scripts]
test = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"
